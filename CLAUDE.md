# Signature Kit (Claude Code)

Read `.agent/instructions.md` for full project instructions, conventions, and architecture details.

Custom skills are available — use `/check`, `/release`, `/test`, `/sync-version`, `/review`, `/add-feature`, `/fix-bug`. All skill definitions live in `.agent/skills/` and are referenced from `.claude/skills/`.
